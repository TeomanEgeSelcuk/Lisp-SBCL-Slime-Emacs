version: '3.8'
services:
  lisp-environment:
    container_name: jupyter-lisp-dev-container
    build:
      context: .
      dockerfile: Dockerfile
    image: jupyter-my-lisp-environment
    volumes:
      - ./src:/src
    tty: true

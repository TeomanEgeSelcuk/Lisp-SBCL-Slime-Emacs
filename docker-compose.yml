version: '3.8'
services:
  lisp-environment:
    container_name: lisp-dev-container
    build:
      context: .
      dockerfile: Dockerfile
    image: my-lisp-environment
    volumes:
      - ./codegrader:/root/quicklisp/local-projects/codegrader
    tty: true
